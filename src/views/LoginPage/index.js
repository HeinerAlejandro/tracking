import React from 'react'

import LoginRegisterUserContainer from './../../containers/LoginRegisterUserContainer'

const LoginPage = (props) => {
	return(
		<LoginRegisterUserContainer />
	)
}

export default LoginPage